#The WP\_Metadata\_Base Class

The `WP__Metadata_Base` class is the ultimate base class for _(almost)_ every class in the #metadata project. It's purpose it to provide a consistent approach to architecting and subclassing classes need for us with #metadata.

